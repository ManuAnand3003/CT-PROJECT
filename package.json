{
    "name": "todo-app",
    "version": "1.0.0",
    "scripts": {
      "build": "webpack --mode production", 
      "lint": "eslint src", 
      "test": "jest", 
      "predeploy": "npm run build && npm run lint && npm test", 
      "deploy": "firebase deploy"
    },
    "dependencies": {
      "firebase": "^10.1.0"
    },
    "devDependencies": {
      "eslint": "^8.0.0",
      "jest": "^29.0.0",
      "webpack": "^5.0.0"
    }
  }